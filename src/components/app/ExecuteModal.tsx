"use client";

/**
 * ExecuteModal is deprecated.
 * Execution is now handled directly in /app/execute/page.tsx
 * using useSignAndExecuteTransaction + buildExecuteActionTx.
 * This file is kept as a stub to avoid breaking any residual imports.
 */
export default function ExecuteModal() {
  return null;
}
